<?xml version="1.0"?>
<configuration>
    <property>
        
{% if hadoop_node_type == 'master' %}
        <name>yarn.resourcemanager.hostname</name>
        <value>0.0.0.0</value>
{% elif hadoop_node_type == 'resourcemanager' %}
        <name>yarn.resourcemanager.hostname</name>
        <value>0.0.0.0</value>
{% elif hadoop_node_type == 'nodemanager' %}
        <name>yarn.resourcemanager.hostname</name>
        <value>{{ hadoop_master }}</value>
{% elif hadoop_node_type == 'slave' %}
        <name>yarn.resourcemanager.hostname</name>
        <value>{{ hadoop_master }}</value>
{% endif %}
    </property>

{% for key, value in yarn_props.iteritems() %}
    <property>
        <name>{{ key }}</name>
        <value>{{ value }}</value>
    </property>
{% endfor %}
</configuration>
